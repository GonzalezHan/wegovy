# WegoBe v4

`v4` is initialized from `v3` so the current storytelling flow, assets, and motion system are preserved as the starting point for the next iteration.

## Commands

```bash
npm install
npm run dev
```

## Scope

- Base source copied from `../v3`
- Independent package name for versioned development
- Ready for further copy, layout, and interaction updates inside `v4/src`
